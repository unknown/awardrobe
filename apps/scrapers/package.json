{
  "name": "@awardrobe/scrapers",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf .turbo node_modules dist",
    "with-env": "dotenv -e ../../.env --",
    "scrapers:run": "npm run with-env -- ts-node -r tsconfig-paths/register --transpile-only src/index.ts"
  },
  "dependencies": {
    "@awardrobe/adapters": "*",
    "@awardrobe/prisma-types": "*",
    "@awardrobe/tsconfig": "*",
    "dotenv": "^16.0.3",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.3.1",
    "dotenv-cli": "^7.2.1",
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  }
}
