{
  "name": "@awardrobe/media-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf .turbo node_modules",
    "deploy": "wrangler deploy --tsconfig=tsconfig.json",
    "dev": "wrangler dev --port=3002 --tsconfig=tsconfig.json --log-level=warn"
  },
  "devDependencies": {
    "@awardrobe/tsconfig": "workspace:^",
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.0.4",
    "wrangler": "^3.23.0"
  }
}
