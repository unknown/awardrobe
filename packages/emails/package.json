{
  "name": "@awardrobe/emails",
  "version": "1.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf .react-email .turbo node_modules dist",
    "emails:preview": "email dev --dir src/emails --port 3001",
    "dev": "npm run build -- --watch",
    "build": "tsup index.ts --format cjs --dts"
  },
  "dependencies": {
    "@react-email/components": "0.0.11",
    "react": "^18.2.0",
    "react-email": "^1.9.5"
  },
  "devDependencies": {
    "@awardrobe/tsconfig": "*",
    "@types/node": "^20.10.1",
    "@types/react": "^18.2.39",
    "tsup": "^8.0.1",
    "typescript": "^5.3.2"
  }
}
