{
  "name": "@awardrobe/adapters",
  "version": "1.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup index.ts --format cjs --dts",
    "dev": "npm run build -- --watch",
    "clean": "rm -rf .react-email .turbo node_modules dist"
  },
  "dependencies": {
    "zod": "^3.21.4"
  },
  "devDependencies": {
    "@awardrobe/tsconfig": "*",
    "@types/node": "^20.3.3",
    "tsup": "^7.0.0",
    "typescript": "^5.1.6"
  }
}
